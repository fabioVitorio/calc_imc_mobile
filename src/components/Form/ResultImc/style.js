import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    result: {
        flex: 1,
        alignItems: "center",
        width: "100%", 
    },
    number: {
        fontSize: 48,
        color: "#000",
        fontWeight: "bold",
        marginBottom: 30
    },
    text: {
        fontSize: 30,
        color: "#000"
    }
})

export default styles